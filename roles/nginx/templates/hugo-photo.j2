server {
       listen 80;
       listen [::]:80;

       server_name mysite.com www.mysite.com;

       root /opt/hugo/public/; #Absolute path to where hugo site is
       index index.html; # Hugo generates HTML

       location / {
               try_files $uri $uri/ =404;
       }
}
